import { Entity } from "../../entity.ts";
import { ASSET_MANAGER } from "../main.ts";


/**
 * Lights for floor
 * @author Emma Szebenyi
 */
export class PlayerLight extends Entity {
  /**
   * Creates a light for the employee
   */
  constructor(
    
  ) {
    super();
    
   
  }
}